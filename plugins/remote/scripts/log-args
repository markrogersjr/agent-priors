#!/usr/bin/env bash
# render the MCP call as Python-style Remote(arg=value, ...) on stdout
jq -r '"Remote(\n" + (.tool_input | to_entries | map("    \(.key)=\(.value | tojson)") | join(",\n")) + "\n)"' || true
